import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CrearpreguntaPage } from './crearpregunta.page';

describe('CrearpreguntaPage', () => {
  let component: CrearpreguntaPage;
  let fixture: ComponentFixture<CrearpreguntaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(CrearpreguntaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
